---
title: SDK Migration Guide
sidebar_position: 1
tags:
  - migration
  - setup
---

# SDK Migration Guide

As [Dyte joins Cloudflare](https://blog.cloudflare.com/introducing-cloudflare-realtime-and-realtimekit), we are transitioning all Dyte SDKs to the new Cloudflare RealtimeKit SDKs.

:::danger
The Dyte SDKs are now in maintenance mode and will no longer receive feature updates or bug fixes. We strongly recommend migrating to the Cloudflare RealtimeKit SDKs to ensure you continue to receive the latest features and improvements. You can find the official documentation for RealtimeKit [here](https://docs.realtime.cloudflare.com/).
:::

### Package Mapping

<Tabs groupId="framework">
  <TabItem value="web-components" label="Web Components">
    The following table maps the deprecated Dyte SDKs to their new Cloudflare RealtimeKit equivalents for Web Components:
    | Dyte SDK                                                                          | RealtimeKit SDK                                                                                   |
    | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
    | [`@dytesdk/web-core`](https://www.npmjs.com/package/@dytesdk/web-core)              | [`@cloudflare/realtimekit`](https://www.npmjs.com/package/@cloudflare/realtimekit)                  |
    | [`@dytesdk/ui-kit`](https://www.npmjs.com/package/@dytesdk/ui-kit)                  | [`@cloudflare/realtimekit-ui`](https://www.npmjs.com/package/@cloudflare/realtimekit-ui)            |
    | [`@dytesdk/ui-kit-addons`](https://www.npmjs.com/package/@dytesdk/ui-kit-addons)    | [`@cloudflare/realtimekit-ui-addons`](https://www.npmjs.com/package/@cloudflare/realtimekit-ui-addons) |
    | [`@dytesdk/video-background-transformer`](https://www.npmjs.com/package/@dytesdk/video-background-transformer) | [`@cloudflare/realtimekit-virtual-background`](https://www.npmjs.com/package/@cloudflare/realtimekit-virtual-background) |
    | [`@dytesdk/recording-sdk`](https://www.npmjs.com/package/@dytesdk/recording-sdk)    | [`@cloudflare/realtimekit-recording-sdk`](https://www.npmjs.com/package/@cloudflare/realtimekit-recording-sdk) |
  </TabItem>
  <TabItem value="react" label="React">
    The following table maps the deprecated Dyte SDKs to their new Cloudflare RealtimeKit equivalents for React:
    | Dyte SDK                                                                          | RealtimeKit SDK                                                                                   |
    | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
    | [`@dytesdk/web-core`](https://www.npmjs.com/package/@dytesdk/web-core)              | [`@cloudflare/realtimekit`](https://www.npmjs.com/package/@cloudflare/realtimekit)                  |
    | [`@dytesdk/react-web-core`](https://www.npmjs.com/package/@dytesdk/react-web-core)  | [`@cloudflare/realtimekit-react`](https://www.npmjs.com/package/@cloudflare/realtimekit-react)      |
    | [`@dytesdk/ui-kit`](https://www.npmjs.com/package/@dytesdk/ui-kit)                  | [`@cloudflare/realtimekit-ui`](https://www.npmjs.com/package/@cloudflare/realtimekit-ui)            |
    | [`@dytesdk/react-ui-kit`](https://www.npmjs.com/package/@dytesdk/react-ui-kit)      | [`@cloudflare/realtimekit-react-ui`](https://www.npmjs.com/package/@cloudflare/realtimekit-react-ui) |
    | [`@dytesdk/ui-kit-addons`](https://www.npmjs.com/package/@dytesdk/ui-kit-addons)    | [`@cloudflare/realtimekit-ui-addons`](https://www.npmjs.com/package/@cloudflare/realtimekit-ui-addons) |
    | [`@dytesdk/video-background-transformer`](https://www.npmjs.com/package/@dytesdk/video-background-transformer) | [`@cloudflare/realtimekit-virtual-background`](https://www.npmjs.com/package/@cloudflare/realtimekit-virtual-background) |
    | [`@dytesdk/recording-sdk`](https://www.npmjs.com/package/@dytesdk/recording-sdk)    | [`@cloudflare/realtimekit-recording-sdk`](https://www.npmjs.com/package/@cloudflare/realtimekit-recording-sdk) |
  </TabItem>
  <TabItem value="angular" label="Angular">
    The following table maps the deprecated Dyte SDKs to their new Cloudflare RealtimeKit equivalents for Angular:
    | Dyte SDK                                                                          | RealtimeKit SDK                                                                                   |
    | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
    | [`@dytesdk/web-core`](https://www.npmjs.com/package/@dytesdk/web-core)              | [`@cloudflare/realtimekit`](https://www.npmjs.com/package/@cloudflare/realtimekit)                  |
    | [`@dytesdk/ui-kit`](https://www.npmjs.com/package/@dytesdk/ui-kit)                  | [`@cloudflare/realtimekit-ui`](https://www.npmjs.com/package/@cloudflare/realtimekit-ui)            |
    | [`@dytesdk/angular-ui-kit`](https://www.npmjs.com/package/@dytesdk/angular-ui-kit)  | [`@cloudflare/realtimekit-angular-ui`](https://www.npmjs.com/package/@cloudflare/realtimekit-angular-ui) |
    | [`@dytesdk/ui-kit-addons`](https://www.npmjs.com/package/@dytesdk/ui-kit-addons)    | [`@cloudflare/realtimekit-ui-addons`](https://www.npmjs.com/package/@cloudflare/realtimekit-ui-addons) |
    | [`@dytesdk/video-background-transformer`](https://www.npmjs.com/package/@dytesdk/video-background-transformer) | [`@cloudflare/realtimekit-virtual-background`](https://www.npmjs.com/package/@cloudflare/realtimekit-virtual-background) |
    | [`@dytesdk/recording-sdk`](https://www.npmjs.com/package/@dytesdk/recording-sdk)    | [`@cloudflare/realtimekit-recording-sdk`](https://www.npmjs.com/package/@cloudflare/realtimekit-recording-sdk) |
  </TabItem>
  <TabItem value="react-native" label="React Native">
    The following table maps the deprecated Dyte SDKs to their new Cloudflare RealtimeKit equivalents for React Native:
    | Dyte SDK                                                                          | RealtimeKit SDK                                                                                   |
    | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
    | [`@dytesdk/web-core`](https://www.npmjs.com/package/@dytesdk/web-core)              | [`@cloudflare/realtimekit`](https://www.npmjs.com/package/@cloudflare/realtimekit)                  |
    | [`@dytesdk/react-native-core`](https://www.npmjs.com/package/@dytesdk/react-native-core)  | [`@cloudflare/realtimekit-react-native`](https://www.npmjs.com/package/@cloudflare/realtimekit-react-native)      |
    | [`@dytesdk/react-native-ui-kit`](https://www.npmjs.com/package/@dytesdk/react-native-ui-kit)      | [`@cloudflare/realtimekit-react-native-ui`](https://www.npmjs.com/package/@cloudflare/realtimekit-react-native-ui) |
  </TabItem>
  <TabItem value="android" label="Android">
    The following table maps the deprecated Dyte SDKs to their new Cloudflare RealtimeKit equivalents for Android:
    | Dyte SDK                                                                          | RealtimeKit SDK                                                                                   |
    | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
    | [`io.dyte:core`](https://central.sonatype.com/artifact/io.dyte/core)              | [`com.cloudflare.realtimekit:core`](https://central.sonatype.com/artifact/com.cloudflare.realtimekit/core) |
    | [`io.dyte:uikit`](https://central.sonatype.com/artifact/io.dyte/uikit)            | [`com.cloudflare.realtimekit:ui-android`](https://central.sonatype.com/artifact/com.cloudflare.realtimekit/ui-android) |
  </TabItem>
  <TabItem value="ios" label="iOS">
    The following table maps the deprecated Dyte SDKs to their new Cloudflare RealtimeKit equivalents for iOS:
    | Dyte SDK       | RealtimeKit SDK                                                   |
    | -------------- | ----------------------------------------------------------------- |
    | `DyteiOSCore`  | [`RealtimeKit`](https://github.com/dyte-in/RealtimeKitCoreiOS)    |
    | `DyteUiKit`    | [`RealtimeKitUI`](https://github.com/dyte-in/RealtimeKitUI)       |

    :::warning
    We have deprecated [Cocoapods](https://blog.cocoapods.org/CocoaPods-Specs-Repo/) support in favor of Swift Package Manager. Please migrate to continue using our SDKs.

    Check out our quickstart guides for [`RealtimeKit`](https://docs.realtime.cloudflare.com/ios-core#step-1-install-the-sdk) and [`RealtimeKitUI`](https://docs.realtime.cloudflare.com/ios/quickstart#step-1-install-the-sdk) for detailed installation instructions.
    :::
  </TabItem>
</Tabs>

## Migration Steps

<Tabs groupId="framework">
  <TabItem value="web-components" label="Web Components">
    Migrating from Dyte to Cloudflare RealtimeKit is straightforward, as it primarily involves updating package names and renaming html tags, JS classes and css selectors.

    To perform the migration manually, please follow the steps outlined below.

    ### 1. Update `package.json`

    First, uninstall all Dyte packages from your project:

    ```bash
    npm uninstall @dytesdk/web-core @dytesdk/ui-kit @dytesdk/ui-kit-addons @dytesdk/video-background-transformer @dytesdk/recording-sdk
    ```

    Next, install the corresponding Cloudflare RealtimeKit packages:

    ```bash
    npm install @cloudflare/realtimekit @cloudflare/realtimekit-ui @cloudflare/realtimekit-ui-addons @cloudflare/realtimekit-virtual-background @cloudflare/realtimekit-recording-sdk
    ```

    ### 2. Update Import Paths

    In your project's source code, perform a find-and-replace for all Dyte package import paths with their new RealtimeKit equivalents.

    | Find                                   | Replace With                               |
    | -------------------------------------- | ------------------------------------------ |
    | `@dytesdk/web-core`                      | `@cloudflare/realtimekit`                  |
    | `@dytesdk/ui-kit`                        | `@cloudflare/realtimekit-ui`               |
    | `@dytesdk/ui-kit-addons`                 | `@cloudflare/realtimekit-ui-addons`        |
    | `@dytesdk/video-background-transformer`  | `@cloudflare/realtimekit-virtual-background` |
    | `@dytesdk/recording-sdk`                 | `@cloudflare/realtimekit-recording-sdk`    |

    ### 3. Update Codebase with New Naming Conventions

    The migration introduces a new set of naming conventions. The general rule is to replace `Dyte` with `RealtimeKit`, `RTK`, or `Rtk`, depending on the context.

    :::info Naming Convention Summary
    - **Main Class**: `Dyte` -> `RealtimeKit` (e.g., `DyteClient` -> `RealtimeKitClient`)
    - **Core SDK Classes & Types**: `Dyte` -> `RTK` (e.g., `DyteSelf` -> `RTKSelf`)
    - **UI Kit Components & Types**: `Dyte` -> `Rtk` (e.g., `DyteMeeting` -> `RtkMeeting`)
    - **CSS Selectors & HTML Tags**: `dyte-` -> `rtk-` (e.g., `dyte-meeting` -> `rtk-meeting`)
    :::

    #### Core SDK Changes

    If you are using the CDN, first step is to replace the Dyte Core SDK script tag with RealtimeKit Core SDK.

    Replace
    ```html
    <script src="https://cdn.dyte.in/core/dyte2.js"></script>
    ```
    with
    ```html
    <script src="https://cdn.jsdelivr.net/npm/@cloudflare/realtimekit@latest/dist/browser.js"></script>
    ```

    If you are importing, `DyteClient` in an `module` script tag, replace it with `RealtimeKitClient`.

    Replace
    ```html
    <script type="module">
    import DyteClient from 'https://cdn.jsdelivr.net/npm/@dytesdk/web-core@latest/dist/index.es.js';
    </script>
    ```
    with
    ```html
    <script type="module">
    import RealtimeKitClient from 'https://cdn.jsdelivr.net/npm/@cloudflare/realtimekit@latest/dist/index.es.js';
    </script>
    ```

    | Find              | Replace With             |
    | ----------------- | ------------------------ |
    | `DyteClient`      | `RealtimeKitClient`      |

    :::info
    Instead of using `latest` version, use the specific version of RealtimeKit SDKs to prevent any unexpected behavior.
    For example, Replace `https://cdn.jsdelivr.net/npm/@cloudflare/realtimekit@latest/dist/index.es.js` with `https://cdn.jsdelivr.net/npm/@cloudflare/realtimekit@1.1.6/dist/index.es.js`.
    :::

    **Classes & Types:**

    ```js
    // Before
    import type { DyteSelf, DyteSelfMedia } from '@dytesdk/web-core';

    // After
    import type { RTKSelf, RTKSelfMedia } from '@cloudflare/realtimekit';
    ```

    | Find            | Replace With   |
    | --------------- | -------------- |
    | `DyteSelf`      | `RTKSelf`      |
    | `DyteSelfMedia` | `RTKSelfMedia` |
    | `DytePlugins`   | `RTKPlugins`   |

    #### UI Kit Changes

    If you are importing, UI Kit in an `module` script tag, replace it with RealtimeKit UI Kit.

    Replace
    ```html
    <script type="module">
    import { defineCustomElements } from 'https://cdn.jsdelivr.net/npm/@cloudflare/realtimekit-ui@latest/loader/index.es2017.js';
    defineCustomElements();
    </script>
    ```
    with
    ```html
    <script type="module">
    import { defineCustomElements } from 'https://cdn.jsdelivr.net/npm/@cloudflare/realtimekit-ui@latest/loader/index.es2017.js';
    defineCustomElements();
    </script>
    ```

    For all UI Kit components, types, and providers, replace `Dyte` with `Rtk`.

    **Components:**

    ```html
    <!-- Before -->
    <dyte-meeting></dyte-meeting>

    <!-- After -->
    <rtk-meeting></rtk-meeting>
    ```

    | Find                  | Replace With         |
    | --------------------- | -------------------- |
    | `dyte-meeting`         | `rtk-meeting`         |
    | `dyte-ui-provider`      | `rtk-ui-provider`      |
    | `dyte-participant-tile` | `rtk-participant-tile` |
    | `dyte-settings`        | `rtk-settings`        |
    | `dyte-dialog-manager`   | `rtk-dialog-manager`   |

    **CSS & HTML:**

    If you have customized components using CSS or are using raw HTML tags, replace the `dyte-` prefix with `rtk-`.

    ```css
    /* Before */
    dyte-meeting {
      background-color: #fff;
    }

    /* After */
    rtk-meeting {
      background-color: #fff;
    }
    ```

    :::note Custom Design System
    If you are using a custom design system, remember to rename `provideDyteDesignSystem` to `provideRtkDesignSystem`.
    :::

    #### Add-on Packages

    - **UI Kit Addons**: For custom elements like `dyte-full-screen-toggle`, rename them to `rtk-full-screen-toggle`.
    - **Video Background Transformer**: This package is now `@cloudflare/realtimekit-virtual-background`. Replace the `DyteVideoBackgroundTransformer` class with `RealtimeKitVideoBackgroundTransformer`.
    - **Recording SDK**: This package is now `@cloudflare/realtimekit-recording-sdk`. Replace the `DyteRecording` class with `RealtimeKitRecording`.

  </TabItem>

  <TabItem value="react" label="React">
    Migrating from Dyte to Cloudflare RealtimeKit is straightforward, as it primarily involves updating package names and renaming components, hooks, and classes.

    :::info Automated Migration
    To automate the migration process, we provide a [CodeMod tool](https://www.npmjs.com/package/@dytesdk/upgrade-to-rtk). This is the recommended approach for a quick and easy upgrade.
    :::

    If you prefer to perform the migration manually or if the CodeMod tool doesn't suit your specific use case, please follow the steps outlined below.

    ### 1. Update `package.json`

    First, uninstall all Dyte packages from your project:

    ```bash
    npm uninstall @dytesdk/web-core @dytesdk/react-web-core @dytesdk/ui-kit @dytesdk/react-ui-kit @dytesdk/ui-kit-addons @dytesdk/video-background-transformer @dytesdk/recording-sdk
    ```

    Next, install the corresponding Cloudflare RealtimeKit packages:

    ```bash
    npm install @cloudflare/realtimekit @cloudflare/realtimekit-react @cloudflare/realtimekit-ui @cloudflare/realtimekit-react-ui @cloudflare/realtimekit-ui-addons @cloudflare/realtimekit-virtual-background @cloudflare/realtimekit-recording-sdk
    ```

    ### 2. Update Import Paths

    In your project's source code, perform a find-and-replace for all Dyte package import paths with their new RealtimeKit equivalents.

    | Find                                   | Replace With                               |
    | -------------------------------------- | ------------------------------------------ |
    | `@dytesdk/web-core`                      | `@cloudflare/realtimekit`                  |
    | `@dytesdk/react-web-core`                | `@cloudflare/realtimekit-react`            |
    | `@dytesdk/ui-kit`                        | `@cloudflare/realtimekit-ui`               |
    | `@dytesdk/react-ui-kit`                  | `@cloudflare/realtimekit-react-ui`         |
    | `@dytesdk/ui-kit-addons`                 | `@cloudflare/realtimekit-ui-addons`        |
    | `@dytesdk/video-background-transformer`  | `@cloudflare/realtimekit-virtual-background` |
    | `@dytesdk/recording-sdk`                 | `@cloudflare/realtimekit-recording-sdk`    |

    ### 3. Update Codebase with New Naming Conventions

    The migration introduces a new set of naming conventions. The general rule is to replace `Dyte` with `RealtimeKit`, `RTK`, or `Rtk`, depending on the context.

    :::info Naming Convention Summary
    - **React Hooks & Providers**: `Dyte` -> `RealtimeKit` (e.g., `useDyteClient` -> `useRealtimeKitClient`)
    - **Core SDK Classes & Types**: `Dyte` -> `RTK` (e.g., `DyteSelf` -> `RTKSelf`)
    - **UI Kit Components & Types**: `Dyte` -> `Rtk` (e.g., `DyteMeeting` -> `RtkMeeting`)
    - **CSS Selectors & HTML Tags**: `dyte-` -> `rtk-` (e.g., `dyte-meeting` -> `rtk-meeting`)
    :::

    #### Core SDK Changes

    Update your React hooks, providers, and Core SDK classes by replacing `Dyte` with `RealtimeKit` or `RTK`.

    **Hooks & Providers:**

    ```js
    // Before
    import { useDyteClient, DyteProvider } from '@dytesdk/react-web-core';

    // After
    import { useRealtimeKitClient, RealtimeKitProvider } from '@cloudflare/realtimekit-react';
    ```

    | Find              | Replace With             |
    | ----------------- | ------------------------ |
    | `DyteClient`      | `RealtimeKitClient`      |
    | `useDyteClient`   | `useRealtimeKitClient`   |
    | `useDyteSelector` | `useRealtimeKitSelector` |
    | `DyteProvider`    | `RealtimeKitProvider`    |

    **Classes & Types:**

    ```js
    // Before
    import type { DyteSelf, DyteSelfMedia } from '@dytesdk/web-core';

    // After
    import type { RTKSelf, RTKSelfMedia } from '@cloudflare/realtimekit';
    ```

    | Find            | Replace With   |
    | --------------- | -------------- |
    | `DyteSelf`      | `RTKSelf`      |
    | `DyteSelfMedia` | `RTKSelfMedia` |
    | `DytePlugins`   | `RTKPlugins`   |

    #### UI Kit Changes

    For all UI Kit components, types, and providers, replace `Dyte` with `Rtk`.

    **Components & Providers:**

    ```js
    // Before
    import { DyteMeeting, DyteParticipantTile } from '@dytesdk/react-ui-kit';

    // After
    import { RtkMeeting, RtkParticipantTile } from '@cloudflare/realtimekit-react-ui';
    ```

    | Find                  | Replace With         |
    | --------------------- | -------------------- |
    | `DyteMeeting`         | `RtkMeeting`         |
    | `DyteUiProvider`      | `RtkUiProvider`      |
    | `DyteParticipantTile` | `RtkParticipantTile` |
    | `DyteSettings`        | `RtkSettings`        |
    | `DyteDialogManager`   | `RtkDialogManager`   |

    **CSS & HTML:**

    If you have customized components using CSS or are using raw HTML tags, replace the `dyte-` prefix with `rtk-`.

    ```css
    /* Before */
    dyte-meeting {
      background-color: #fff;
    }

    /* After */
    rtk-meeting {
      background-color: #fff;
    }
    ```

    :::note Custom Design System
    If you are using a custom design system, remember to rename `provideDyteDesignSystem` to `provideRtkDesignSystem`.
    :::

    #### Add-on Packages

    - **UI Kit Addons**: For custom elements like `dyte-full-screen-toggle`, rename them to `rtk-full-screen-toggle`.
    - **Video Background Transformer**: This package is now `@cloudflare/realtimekit-virtual-background`. Replace the `DyteVideoBackgroundTransformer` class with `RealtimeKitVideoBackgroundTransformer`.
    - **Recording SDK**: This package is now `@cloudflare/realtimekit-recording-sdk`. Replace the `DyteRecording` class with `RealtimeKitRecording`.

  </TabItem>
  <TabItem value="angular" label="Angular">
    Migrating from Dyte to Cloudflare RealtimeKit is straightforward, as it primarily involves updating package names and renaming html tags, JS classes and css selectors.

    To perform the migration manually, please follow the steps outlined below.

    ### 1. Update `package.json`

    First, uninstall all Dyte packages from your project:

    ```bash
    npm uninstall @dytesdk/web-core @dytesdk/ui-kit @dytesdk/angular-ui-kit @dytesdk/ui-kit-addons @dytesdk/video-background-transformer @dytesdk/recording-sdk
    ```

    Next, install the corresponding Cloudflare RealtimeKit packages:

    ```bash
    npm install @cloudflare/realtimekit @cloudflare/realtimekit-ui @cloudflare/realtimekit-angular-ui @cloudflare/realtimekit-ui-addons @cloudflare/realtimekit-virtual-background @cloudflare/realtimekit-recording-sdk
    ```

    ### 2. Update Import Paths

    In your project's source code, perform a find-and-replace for all Dyte package import paths with their new RealtimeKit equivalents.

    | Find                                   | Replace With                               |
    | -------------------------------------- | ------------------------------------------ |
    | `@dytesdk/web-core`                      | `@cloudflare/realtimekit`                  |
    | `@dytesdk/ui-kit`                        | `@cloudflare/realtimekit-ui`               |
    | `@dytesdk/angular-ui-kit`                | `@cloudflare/realtimekit-angular-ui`       |
    | `@dytesdk/ui-kit-addons`                 | `@cloudflare/realtimekit-ui-addons`        |
    | `@dytesdk/video-background-transformer`  | `@cloudflare/realtimekit-virtual-background` |
    | `@dytesdk/recording-sdk`                 | `@cloudflare/realtimekit-recording-sdk`    |

    ### 3. Update Codebase with New Naming Conventions

    The migration introduces a new set of naming conventions. The general rule is to replace `Dyte` with `RealtimeKit`, `RTK`, or `Rtk`, depending on the context.

    :::info Naming Convention Summary
    - **Main Class**: `Dyte` -> `RealtimeKit` (e.g., `DyteClient` -> `RealtimeKitClient`)
    - **Core SDK Classes & Types**: `Dyte` -> `RTK` (e.g., `DyteSelf` -> `RTKSelf`)
    - **UI Kit Components & Types**: `Dyte` -> `Rtk` (e.g., `DyteMeeting` -> `RtkMeeting`)
    - **CSS Selectors & HTML Tags**: `dyte-` -> `rtk-` (e.g., `dyte-meeting` -> `rtk-meeting`)
    :::

    #### Core SDK Changes

    Replace
    ```html
    <script type="module">
    import DyteClient from '@dytesdk/web-core';
    </script>
    ```
    with
    ```html
    <script type="module">
    import RealtimeKitClient from '@cloudflare/realtimekit';
    </script>
    ```

    | Find              | Replace With             |
    | ----------------- | ------------------------ |
    | `DyteClient`      | `RealtimeKitClient`      |

    **Classes & Types:**

    ```js
    // Before
    import type { DyteSelf, DyteSelfMedia } from '@dytesdk/web-core';

    // After
    import type { RTKSelf, RTKSelfMedia } from '@cloudflare/realtimekit';
    ```

    | Find            | Replace With   |
    | --------------- | -------------- |
    | `DyteSelf`      | `RTKSelf`      |
    | `DyteSelfMedia` | `RTKSelfMedia` |
    | `DytePlugins`   | `RTKPlugins`   |

    #### UI Kit Changes

    To be able to use UI Kit components, instead of `DyteComponentsModule`, import `RealtimeKitComponentsModule` from `@cloudflare/realtimekit-angular-ui`.

    Replace
    ```js
    import { DyteComponentsModule } from '@dytesdk/angular-ui-kit';
    ```
    with
    ```js
    import { RealtimeKitComponentsModule } from '@cloudflare/realtimekit-angular-ui';
    ```

    For all UI Kit components, types, and providers, replace `Dyte` with `Rtk`.

    **Components:**

    ```html
    <!-- Before -->
    <dyte-meeting></dyte-meeting>

    <!-- After -->
    <rtk-meeting></rtk-meeting>
    ```

    | Find                  | Replace With         |
    | --------------------- | -------------------- |
    | `DyteComponentsModule` | `RealtimeKitComponentsModule` |
    | `dyte-meeting`         | `rtk-meeting`         |
    | `dyte-ui-provider`      | `rtk-ui-provider`      |
    | `dyte-participant-tile` | `rtk-participant-tile` |
    | `dyte-settings`        | `rtk-settings`        |
    | `dyte-dialog-manager`   | `rtk-dialog-manager`   |

    **CSS & HTML:**

    If you have customized components using CSS or are using raw HTML tags, replace the `dyte-` prefix with `rtk-`.

    ```css
    /* Before */
    dyte-meeting {
      background-color: #fff;
    }

    /* After */
    rtk-meeting {
      background-color: #fff;
    }
    ```

    :::note Custom Design System
    If you are using a custom design system, remember to rename `provideDyteDesignSystem` to `provideRtkDesignSystem`.
    :::

    #### Add-on Packages

    - **UI Kit Addons**: For custom elements like `dyte-full-screen-toggle`, rename them to `rtk-full-screen-toggle`.
    - **Video Background Transformer**: This package is now `@cloudflare/realtimekit-virtual-background`. Replace the `DyteVideoBackgroundTransformer` class with `RealtimeKitVideoBackgroundTransformer`.
    - **Recording SDK**: This package is now `@cloudflare/realtimekit-recording-sdk`. Replace the `DyteRecording` class with `RealtimeKitRecording`.

  </TabItem>

  <TabItem value="react-native" label="React Native">
    Migrating from Dyte to Cloudflare RealtimeKit is straightforward, as it primarily involves updating package names and renaming components, hooks, and classes.

    To perform the migration manually, please follow the steps outlined below.

    ### 1. Update `package.json`

    First, uninstall all Dyte packages from your project:

    ```bash
    npm uninstall @dytesdk/web-core @dytesdk/react-native-core @dytesdk/react-native-ui-kit @dyteinternals/react-native-webrtc
    ```

    Next, Uninstall all the old ui-kit dependencies from your project:

    ```bash
    npm uninstall react-native-document-picker react-native-file-viewer react-native-fs react-native-safe-area-context react-native-sound-player react-native-svg react-native-webview
    ```

    Next install all the new ui-kit dependencies:

    ```bash
    npm install @cloudflare/react-native-webrtc @react-native-documents/picker react-native-file-viewer react-native-fs react-native-sound-player react-native-webview
    ```

    Next, install the corresponding Cloudflare RealtimeKit packages:

    ```bash
    npm install @cloudflare/realtimekit-react-native @cloudflare/realtimekit-react-native-ui
    ```

    Install these **required** dependencies as per your ```react-native``` version.

    - ```react-native-safe-area-context```
      - **react-native (0.64 - 0.74)** :
        ```bash
        npm install react-native-safe-area-context@^4.0.0
        ```
      - **react-native (>= 0.74)** :
        ```bash
        npm install react-native-safe-area-context@^5.0.0
        ```

    - ```react-native-svg```
      - Follow the [installation instructions](https://github.com/react-native-svg/react-native-svg#installation) for react-native-svg.
      - ```bash
        npm install react-native-svg@<compatible-version>
        ```

    ### 2. Update Import Paths

    In your project's source code, perform a find-and-replace for all Dyte package import paths with their new RealtimeKit equivalents.

    | Find                                   | Replace With                               |
    | -------------------------------------- | ------------------------------------------ |
    | `@dytesdk/web-core`                      | `@cloudflare/realtimekit`                  |
    | `@dytesdk/react-native-core`             | `@cloudflare/realtimekit-react-native`|
    | `@dytesdk/react-native-ui-kit`           | `@cloudflare/realtimekit-react-native-ui`|

    ### 3. Update Codebase with New Naming Conventions

    The migration introduces a new set of naming conventions. The general rule is to replace `Dyte` with `RealtimeKit`, `RTK`, or `Rtk`, depending on the context.

    :::info Naming Convention Summary
    - **React Hooks & Providers**: `Dyte` -> `RealtimeKit` (e.g., `useDyteClient` -> `useRealtimeKitClient`)
    - **Core SDK Classes & Types**: `Dyte` -> `RTK` (e.g., `DyteSelf` -> `RTKSelf`)
    - **UI Kit Components & Types**: `Dyte` -> `Rtk` (e.g., `DyteMeeting` -> `RtkMeeting`)
    - **CSS Selectors & HTML Tags**: `dyte-` -> `rtk-` (e.g., `dyte-meeting` -> `rtk-meeting`)
    :::

    #### Core SDK Changes

    Update your React hooks, providers, and Core SDK classes by replacing `Dyte` with `RealtimeKit` or `RTK`.

    **Hooks & Providers:**

    ```js
    // Before
    import { useDyteClient, DyteProvider } from '@dytesdk/react-native-core';

    // After
    import { useRealtimeKitClient, RealtimeKitProvider } from '@cloudflare/realtimekit-react-native';
    ```

    | Find              | Replace With             |
    | ----------------- | ------------------------ |
    | `DyteClient`      | `RealtimeKitClient`      |
    | `useDyteClient`   | `useRealtimeKitClient`   |
    | `useDyteSelector` | `useRealtimeKitSelector` |
    | `DyteProvider`    | `RealtimeKitProvider`    |

    **Classes & Types:**

    ```js
    // Before
    import type { DyteSelf, DyteSelfMedia } from '@dytesdk/react-native-core';

    // After
    import type { RTKSelf, RTKSelfMedia } from '@cloudflare/realtimekit-react-native';
    ```

    | Find            | Replace With   |
    | --------------- | -------------- |
    | `DyteSelf`      | `RTKSelf`      |
    | `DyteSelfMedia` | `RTKSelfMedia` |
    | `DytePlugins`   | `RTKPlugins`   |

    #### UI Kit Changes

    For all UI Kit components, types, and providers, replace `Dyte` with `Rtk`.

    **Components & Providers:**

    ```js
    // Before
    import { DyteMeeting, DyteParticipantTile } from '@dytesdk/react-native-ui-kit';

    // After
    import { RtkMeeting, RtkParticipantTile } from '@cloudflare/realtimekit-react-native-ui';
    ```

    | Find                  | Replace With         |
    | --------------------- | -------------------- |
    | `DyteMeeting`         | `RtkMeeting`         |
    | `DyteUiProvider`      | `RtkUiProvider`      |
    | `DyteParticipantTile` | `RtkParticipantTile` |
    | `DyteSettings`        | `RtkSettings`        |
    | `DyteDialogManager`   | `RtkDialogManager`   |

    **CSS & HTML:**

    If you have customized components using CSS or are using raw HTML tags, replace the `dyte-` prefix with `rtk-`.

    ```css
    /* Before */
    dyte-meeting {
      background-color: #fff;
    }

    /* After */
    rtk-meeting {
      background-color: #fff;
    }
    ```

    :::note Custom Design System
    If you are using a custom design system, remember to rename `provideDyteDesignSystem` to `provideRtkDesignSystem`.
    :::
  </TabItem>
  <TabItem value="android" label="Android">
    Migrating from Dyte to Cloudflare RealtimeKit is straightforward, as it primarily involves updating package names and renaming imports and type prefixes.

    To perform the migration manually, please follow the steps outlined below.

    ### 1. Update package coordinates

    Use the table above to update the package coordinates to the latest RealtimeKit versions.

    ### 2. Update import paths

    In your project's source code, perform a find-and-replace for all Dyte package import paths with their new RealtimeKit equivalents.

    | Find                                   | Replace With                               |
    | -------------------------------------- | ------------------------------------------ |
    | `io.dyte.core`                         | `com.cloudflare.realtimekit`               |
    | `dyte.io.uikit`                        | `com.cloudflare.realtimekit.ui`            |

    ### 3. Update codebase to new naming conventions

    The migration introduces a new set of naming conventions. The general rule is to replace `Dyte` with `Rtk`.

    :::info Naming Convention Summary
    - **Main Class**: `Dyte` -> `RealtimeKit` (e.g., `DyteClient` -> `RealtimeKitClient`)
    - **Core SDK Classes & Types**: `Dyte` -> `Rtk` (e.g., `DyteSelfParticipant` -> `RtkSelfParticipant`)
    - **UI Kit Components & Types**: `Dyte` -> `Rtk` (e.g., `DyteLabel` -> `RtkLabel`)
    :::

    As part of the migration to RealtimeKit, we have also simplified the package structure so there will be more manual changes required after a bulk replace. A non-exhaustive table of these changes is provided below for your reference.

    | Find                                                                | Replace With                                                           |
    | ------------------------------------------------------------------- | ---------------------------------------------------------------------- |
    | `com.cloudflare.realtimekit.RtkPluginWebView`                       | `com.cloudflare.realtimekit.plugins.RtkPluginWebView`                  |
    | `com.cloudflare.realtimekit.VideoView`                              | `com.cloudflare.realtimekit.platform.VideoView`                        |
    | `com.cloudflare.realtimekit.controllers.MeetingConfig`              | `com.cloudflare.realtimekit.meta.MeetingConfig`                        |
    | `com.cloudflare.realtimekit.controllers.RtkLivestreamData`          | `com.cloudflare.realtimekit.livestream.RtkLivestreamData`              |
    | `com.cloudflare.realtimekit.controllers.RtkRecordingState`          | `com.cloudflare.realtimekit.recording.RecordingState`                  |
    | `com.cloudflare.realtimekit.controllers.StageStatus`                | `com.cloudflare.realtimekit.stage.StageStatus`                         |
    | `com.cloudflare.realtimekit.controllers.polls.IPollsController`     | `com.cloudflare.realtimekit.polls.RtkPolls`                            |
    | `com.cloudflare.realtimekit.feat.RealtimeKitChatExtensionsKt`       | `com.cloudflare.realtimekit.chat.RealtimeKitChatExtensionsKt`          |
    | `com.cloudflare.realtimekit.feat.RtkChat`                           | `com.cloudflare.realtimekit.chat.RtkChat`                              |
    | `com.cloudflare.realtimekit.feat.RtkChatMessage`                    | `com.cloudflare.realtimekit.chat.ChatMessage`                          |
    | `com.cloudflare.realtimekit.feat.RtkChatMessageKt`                  | `com.cloudflare.realtimekit.chat.ChatMessageKt`                        |
    | `com.cloudflare.realtimekit.feat.RtkFileMessage`                    | `com.cloudflare.realtimekit.chat.FileMessage`                          |
    | `com.cloudflare.realtimekit.feat.RtkImageMessage`                   | `com.cloudflare.realtimekit.chat.ImageMessage`                         |
    | `com.cloudflare.realtimekit.feat.RtkLivestream`                     | `com.cloudflare.realtimekit.livestream.RtkLivestream`                  |
    | `com.cloudflare.realtimekit.feat.RtkMessageType`                    | `com.cloudflare.realtimekit.chat.ChatMessageType`                      |
    | `com.cloudflare.realtimekit.feat.RtkMeta`                           | `com.cloudflare.realtimekit.meta.RtkMeta`                              |
    | `com.cloudflare.realtimekit.feat.RtkPlugin`                         | `com.cloudflare.realtimekit.plugins.RtkPlugin`                         |
    | `com.cloudflare.realtimekit.feat.RtkPlugins`                        | `com.cloudflare.realtimekit.plugins.RtkPlugins`                        |
    | `com.cloudflare.realtimekit.feat.RtkPoll`                           | `com.cloudflare.realtimekit.polls.Poll`                                |
    | `com.cloudflare.realtimekit.feat.RtkPollOption`                     | `com.cloudflare.realtimekit.polls.PollOption`                          |
    | `com.cloudflare.realtimekit.feat.RtkPollVote`                       | `com.cloudflare.realtimekit.polls.PollVote`                            |
    | `com.cloudflare.realtimekit.feat.RtkPolls`                          | `com.cloudflare.realtimekit.polls.RtkPolls`                            |
    | `com.cloudflare.realtimekit.feat.RtkRecording`                      | `com.cloudflare.realtimekit.recording.RtkRecording`                    |
    | `com.cloudflare.realtimekit.feat.RtkStage`                          | `com.cloudflare.realtimekit.stage.RtkStage`                            |
    | `com.cloudflare.realtimekit.feat.RtkTextMessage`                    | `com.cloudflare.realtimekit.chat.TextMessage`                          |
    | `com.cloudflare.realtimekit.listeners.ExternalEventListener`        | `com.cloudflare.realtimekit.events.ExternalEventListener`              |
    | `com.cloudflare.realtimekit.listeners.RtkChatEventsListener`        | `com.cloudflare.realtimekit.chat.RtkChatEventListener`                 |
    | `com.cloudflare.realtimekit.listeners.RtkConnectionEventListener`   | `com.cloudflare.realtimekit.RtkConnectionEventListener`                |
    | `com.cloudflare.realtimekit.listeners.RtkDataUpdateListener`        | `com.cloudflare.realtimekit.events.RtkDataUpdateListener`              |
    | `com.cloudflare.realtimekit.listeners.RtkLivestreamEventListener`   | `com.cloudflare.realtimekit.livestream.RtkLivestreamEventListener`     |
    | `com.cloudflare.realtimekit.listeners.RtkParticipantUpdateListener` | `com.cloudflare.realtimekit.participants.RtkParticipantUpdateListener` |
    | `com.cloudflare.realtimekit.listeners.RtkParticipantsEventListener` | `com.cloudflare.realtimekit.participants.RtkParticipantsEventListener` |
    | `com.cloudflare.realtimekit.listeners.RtkPluginEventsListener`      | `com.cloudflare.realtimekit.plugins.RtkPluginsEventListener`           |
    | `com.cloudflare.realtimekit.listeners.RtkPollsEventListener`        | `com.cloudflare.realtimekit.polls.RtkPollsEventListener`               |
    | `com.cloudflare.realtimekit.listeners.RtkRecordingEventsListener`   | `com.cloudflare.realtimekit.recording.RtkRecordingEventListener`       |
    | `com.cloudflare.realtimekit.listeners.RtkSelfEventsListener`        | `com.cloudflare.realtimekit.self.RtkSelfEventListener`                 |
    | `com.cloudflare.realtimekit.listeners.RtkStageEventListener`        | `com.cloudflare.realtimekit.stage.RtkStageEventListener`               |
    | `com.cloudflare.realtimekit.listeners.RtkWaitlistEventsListener`    | `com.cloudflare.realtimekit.waitingroom.RtkWaitlistEventListener`      |
    | `com.cloudflare.realtimekit.media.ProducerType`                     | `com.cloudflare.realtimekit.media.sfu.ProducerType`                    |
    | `com.cloudflare.realtimekit.models.ActiveTabType`                   | `com.cloudflare.realtimekit.spotlight.ActiveTabType`                   |
    | `com.cloudflare.realtimekit.models.AudioDeviceType`                 | `com.cloudflare.realtimekit.media.AudioDeviceType`                     |
    | `com.cloudflare.realtimekit.models.RtkParticipant`                  | `com.cloudflare.realtimekit.RtkParticipant`                            |
    | `com.cloudflare.realtimekit.models.RtkParticipants`                 | `com.cloudflare.realtimekit.participants.RtkParticipants`              |
    | `com.cloudflare.realtimekit.models.RtkSelfParticipant`              | `com.cloudflare.realtimekit.self.RtkSelfParticipant`                   |
    | `com.cloudflare.realtimekit.models.VideoDeviceType`                 | `com.cloudflare.realtimekit.media.VideoDeviceType`                     |
    | `com.cloudflare.realtimekit.models.VideoScaleType`                  | `com.cloudflare.realtimekit.media.VideoScaleType`                      |
    | `com.cloudflare.realtimekit.models.WaitListStatus`                  | `com.cloudflare.realtimekit.waitingroom.WaitListStatus`                |
    | `com.cloudflare.realtimekit.network.info.GridInfo`                  | `com.cloudflare.realtimekit.participants.GridInfo`                     |
    | `com.cloudflare.realtimekit.participants.ParticipantFlags`          | `com.cloudflare.realtimekit.ParticipantFlags`                          |
    | `com.cloudflare.realtimekit.participants.RtkMeetingParticipant`     | `com.cloudflare.realtimekit.RtkMeetingParticipant`                     |
    | `com.cloudflare.realtimekit.platform.RtkPluginFile`                 | `com.cloudflare.realtimekit.plugins.RtkPluginFile`                     |

  </TabItem>

  <TabItem value="ios" label="iOS">
      Migrating from Dyte to Cloudflare RealtimeKit is a straightforward process that mainly involves updating package names and renaming components and classes.

      To manually migrate your project, follow these steps:

      ### 1. Update Package Imports

      In your source code, replace all Dyte package import paths with their corresponding RealtimeKit equivalents:

      | Find          | Replace With    |
      | ------------- | --------------- |
      | `DyteiOSCore` | `RealtimeKit`   |
      | `DyteUiKit`   | `RealtimeKitUI` |

      ### 2. Update Naming Conventions

      RealtimeKit introduces new naming conventions. Generally, replace `Dyte` with `RealtimeKit`, `RTK`, or `Rtk`, depending on the context.

      :::info Naming Convention Summary
      - **Main Class**: `Dyte` -> `RealtimeKit` (e.g., `DyteClient` -> `RealtimeKitClient`)
      - **Core SDK Classes & Types**: Replace `Dyte` with `Rtk` (e.g., `DyteMeetingType` → `RtkMeetingType`)
      - **UI Kit Components & Types**: Replace `Dyte` with `Rtk` (e.g., `DyteControlBarButton` → `RtkControlBarButton`)
      :::

      #### Core SDK Changes

      To upgrade your codebase from Dyte to RealtimeKit (RTK), update your classes and listeners as follows. This guide highlights the most common changes; refer to the official documentation for exhaustive details.

      ##### Class Renaming

      | Find                            | Replace With                 |
      |---------------------------------|------------------------------|
      | `DyteClient`                    | `RealtimeKitClient`          |
      | `DyteSelfParticipant`           | `RtkSelfParticipant`         |
      | `DyteJoinedMeetingParticipant`  | `RtkRemoteParticipant`       |

      ##### Listener Interface Renaming

      Many event listener interfaces have been renamed for consistency. Update your implementations accordingly:

      | Find                             | Replace With                  |
      |----------------------------------|-------------------------------|
      | `DyteSelfEventsListener`         | `RtkSelfEventListener`        |
      | `DyteChatEventsListener`         | `RtkChatEventListener`        |
      | `DyteLivestreamEventListener`    | `RtkLivestreamEventListener`  |
      | `DyteMeetingRoomEventsListener`  | `RtkMeetingRoomEventListener` |
      | `DyteParticipantsEventListener`  | `RtkParticipantsEventListener`|
      | `DyteParticipantUpdateListener`  | `RtkParticipantUpdateListener`|
      | `DytePluginEventsListener`       | `RtkPluginsEventListener`     |
      | `DytePollsEventListener`         | `RtkPollsEventListener`       |
      | `DyteRecordingEventsListener`    | `RtkRecordingEventListener`   |
      | `DyteStageEventListener`         | `RtkStageEventListener`       |
      | `DyteWaitlistEventsListener`     | `RtkWaitlistEventListener`    |
      | `DyteDataUpdateListener`         | `RtkDataUpdateListener`       |

      Constructor parameters such as `mobileClient` have also been renamed to `rtkClient`, among others.

      #### UI Kit Changes

      For all UI Kit components, types, and providers, replace `Dyte` with `Rtk` in their names.
  </TabItem>
</Tabs>
